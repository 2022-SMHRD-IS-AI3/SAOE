<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.saoe.mapper.MemberMapper">

	<insert id="join" parameterType="MemberDTO">
		insert into member (id, pw, name, nick, tel, addr, birth, gender, profile, member_score, grade) values (#{id}, #{pw}, #{name}, #{nick}, #{tel}, #{addr}, #{birth}, #{gender}, #{profile}, #{member_score}, #{grade})		
	</insert>
	
	<select id="login" parameterType="MemberDTO" resultType="MemberDTO">
		select * from member where id = #{id} and pw = #{pw}
	</select>

</mapper>